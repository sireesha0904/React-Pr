function Footer() {
    return (
        <footer>
            <p>&copy; {new Date().getFullYear()} My React App</p>
        </footer>
    );
}

export default Footer;
// This is a simple React component that serves as a footer for the application.